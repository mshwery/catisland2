<!DOCTYPE html>
<html>
<head>
  <title>Gather</title>
  <meta name="description" content="New Orleans news, personalized.">
  <meta name="author" content="@mshwery, @andrewishungry">
  <meta name="viewport" content="width=device-width, user-scalable=0, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0" />
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Oxygen:300,400,700|Open+Sans:300,400,600' rel='stylesheet' type='text/css'>
</head>
<body class="<%= body_class %>">

  <header>
    <nav class="cf">
      <%= link_to 'Articles', articles_path %>
      <%= link_to 'Manage', manage_path %>

      <div class="account right">
        <% if current_user %>
          <%= link_to current_user.name, '#' %>
          <%= link_to 'Logout', logout_path %>
        <% else %>
          <%= link_to 'Login', login_path %>
          <%= link_to 'Sign up', new_identity_path %>
        <% end %>
      </div>
    </nav>
  </header>

  <div id="main">
    <%= yield %>
  </div>

</body>
</html>
